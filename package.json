{
  "name": "target",
  "version": "0.1.0",
  "description": "The Target Project: define workflows made of sequential steps, each running as a job against one dedicated agent+hook (agent-webhook-bridge) on a shared Claude session.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "target:install": "node scripts/bootstrap.mjs",
    "start": "node scripts/start.ts",
    "test": "npm --prefix hub test",
    "typecheck": "npm --prefix hub run typecheck && npm --prefix hub/ui run typecheck",
    "ui:dev": "npm --prefix hub/ui run dev",
    "ui:build": "npm --prefix hub/ui run build"
  }
}
