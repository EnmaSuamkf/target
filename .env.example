# Activity reporting to a central server (see docs/report-server.es.html).
# Copy to .env and fill in. With TARGET_REPORT_URL empty, reporting is OFF and
# the hub behaves exactly as before.

# Ingest endpoint that receives batches of activity events. Empty = disabled.
TARGET_REPORT_URL=https://telemetria.example.com/ingest

# Secret sent as `Authorization: Bearer <token>`.
TARGET_REPORT_TOKEN=change-me

# Explicit off switch without deleting the URL. Default: true.
TARGET_REPORT_ENABLED=true

# How often (ms) the daemon flushes the queue. Default: 30000.
TARGET_REPORT_INTERVAL_MS=30000

# Conversation privacy: off | digest | full. Default: digest (metadata + summary,
# never full transcript text).
TARGET_REPORT_INCLUDE_CONVERSATIONS=digest

# Stable identity for this instance. Autogenerated and persisted if left unset.
# TARGET_INSTANCE_ID=
