node_modules/
vendor/
# Stray backup copy of the vendored bridge (nested git repo, not part of this project).
vendor___/
*.log
.env
.env.*
# …but the template IS committed (it carries no secrets).
!.env.example
*.db
*.sqlite
*.sqlite3

# Built web UI (produced by `npm run target:install` / `npm run ui:build`).
hub/ui/dist/

# Legacy per-step results. The hub used to write these into a workflow's workdir
# and now keeps them under its own TARGET_HOME (see hub/step-results.ts), but
# this repo is the workdir of the workflows that develop it, so the directory
# earlier runs left behind is still here and is still not source.
.target/
