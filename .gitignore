node_modules/
vendor/
# Stray backup copy of the vendored bridge (nested git repo, not part of this project).
vendor___/
*.log
.env
.env.*
# …but the template IS committed (it carries no secrets).
!.env.example
*.db
*.sqlite
*.sqlite3

# Built web UI (produced by `npm run target:install` / `npm run ui:build`).
hub/ui/dist/

# Per-step results the hub writes into a workflow's workdir (see
# hub/step-results.ts). This repo is itself the workdir of the workflows that
# develop it, so its own runs land here: they are one agent's notes to the
# next, not source, and they change on every run.
.target/
